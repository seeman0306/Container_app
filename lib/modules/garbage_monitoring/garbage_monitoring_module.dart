import 'package:flutter/material.dart';

class GarbageMonitoringModule extends StatelessWidget {
  const GarbageMonitoringModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Garbage Monitoring")),
      body: const Center(
        child: Text("Garbage Monitoring Module Placeholder"),
      ),
    );
  }
}
