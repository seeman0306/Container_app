import 'package:flutter/material.dart';

class HealthManagementModule extends StatelessWidget {
  const HealthManagementModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Health Management")),
      body: const Center(
        child: Text("Health Management Module Placeholder"),
      ),
    );
  }
}
