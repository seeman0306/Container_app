import 'package:flutter/material.dart';

class SolarPowerModule extends StatelessWidget {
  const SolarPowerModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Solar Power")),
      body: const Center(
        child: Text("Solar Power Module Placeholder"),
      ),
    );
  }
}
