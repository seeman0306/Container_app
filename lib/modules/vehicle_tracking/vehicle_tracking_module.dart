import 'package:flutter/material.dart';

class VehicleTrackingModule extends StatelessWidget {
  const VehicleTrackingModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Vehicle Tracking")),
      body: const Center(
        child: Text("Vehicle Tracking Module Placeholder"),
      ),
    );
  }
}
