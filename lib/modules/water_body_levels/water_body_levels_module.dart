import 'package:flutter/material.dart';

class WaterBodyLevelsModule extends StatelessWidget {
  const WaterBodyLevelsModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("WaterBody Levels")),
      body: const Center(
        child: Text("WaterBody Levels Module Placeholder"),
      ),
    );
  }
}
