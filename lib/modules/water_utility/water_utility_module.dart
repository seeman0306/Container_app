import 'package:flutter/material.dart';

class WaterUtilityModule extends StatelessWidget {
  const WaterUtilityModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Water Utility")),
      body: const Center(
        child: Text("Water Utility Module Placeholder"),
      ),
    );
  }
}
