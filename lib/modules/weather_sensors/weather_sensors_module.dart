import 'package:flutter/material.dart';

class WeatherSensorsModule extends StatelessWidget {
  const WeatherSensorsModule({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Weather Sensors")),
      body: const Center(
        child: Text("Weather Sensors Module Placeholder"),
      ),
    );
  }
}
